1) clone the project using : git clone https://github.com/Rudhra0219/qn-1.git

2) after cloning the project, open project in vs code and run this :  mvn clean test  

3) then create a new repository and push to git hub using the following commands
git init
git add .
git commit -m "Initial commit - hotel booking system with CI/CD"
git remote add origin https://github.com/Rudhra0219/yourreponame.git
git branch -M main
git push -u origin main

4) create a pipeline job in jenkins and  paste the pipeline code and click save and build now

5) i attached two new files deployment.yaml and service.yaml -- add this in you new repository and the folder you have cloned in your system and run the following commands in vs code 
kubectl apply -f deployment.yaml
kubectl apply -f service.yaml


6)To verify its running:
kubectl get pods
kubectl get services

paste the screenshot of the above steps clearly

optional: 7) Access Your App
Open browser:
http://localhost:30007


ethuna doubtna edit kuduthu inga type pannu
